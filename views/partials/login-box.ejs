<div class="bg-white shadow rounded-lg p-6 mb-6">
  <div class="flex items-center justify-between mb-3">
    <h3 class="text-base font-semibold">สำหรับสมาชิก</h3>
    <% if (user) { %>
      <span class="inline-flex items-center text-xs px-2 py-0.5 rounded bg-green-100 text-green-700">
        ลงชื่อเข้าใช้แล้ว
      </span>
    <% } %>
  </div>

  <% if (!user) { %>
    <!-- ยังไม่ล็อกอิน: กดเพื่อไปหน้าเข้าสู่ระบบ -->
    <a href="/login"
       class="inline-flex items-center justify-center w-full rounded py-2 text-sm font-medium text-white hover:opacity-90 focus:ring-2 focus:ring-green-300"
       style="background-color:#16A34A;">
      เข้าสู่ระบบสำหรับสมาชิก
    </a>
    <p class="mt-2 text-xs text-gray-500 text-center">
      ยังไม่มีบัญชี? <a href="/register" class="text-blue-600 hover:underline">สมัครสมาชิก</a>
    </p>
  <% } else { %>
    <!-- ล็อกอินแล้ว: โชว์ลิงก์ที่เกี่ยวข้อง -->
    <div class="grid grid-cols-1 gap-2 text-sm">
      <a href="/account" class="rounded border px-3 py-2 hover:bg-gray-50">โปรไฟล์ของฉัน</a>
      <a href="/account/security" class="rounded border px-3 py-2 hover:bg-gray-50">ความปลอดภัย</a>
    </div>
    <div class="pt-3">
      <a href="/logout"
         class="inline-flex items-center justify-center w-full rounded py-2 text-sm font-medium bg-red-600 text-white hover:bg-red-700 focus:ring-2 focus:ring-red-400">
        ออกจากระบบ
      </a>
    </div>
  <% } %>
</div>
