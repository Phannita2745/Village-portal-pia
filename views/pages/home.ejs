<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>หน้าแรก</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-blue-50">

  <!-- ✅ Navbar -->
  <%- include('../partials/navbar') %>

  <main class="pt-20 max-w-6xl mx-auto p-6">
    <section class="mt-6 mb-6">
      <!-- ✅ สไลด์ข่าว -->
      <%- include('../partials/slider', { news: news }) %>

      <!-- ✅ ส่วนเนื้อหา -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <!-- 📰 ข่าว + 📅 กิจกรรม -->
        <div class="md:col-span-2">
          <!-- 📢 ประกาศ (โปสเตอร์) -->
          <section class="bg-white p-4 shadow rounded mb-6">
            <h2 class="text-lg font-semibold mb-3">📢 ประกาศ</h2>
            <div class="flex justify-center">
              <img src="/uploads/Poster.jpg" alt="โปสเตอร์รณรงค์ป้องกันไข้เลือดออก"
                  class="w-full max-w-xl h-auto object-contain rounded"/>
            </div>
          </section>

          <!-- 📣 ข่าวประชาสัมพันธ์ -->
          <%- include('../partials/news-section', { news: news }) %>

          <!-- 📢 ข่าวประชาสัมพันธ์เฉพาะสมาชิก -->
          <%- include('../partials/activities-section', { activities: activities, user: user }) %>
        </div>

        <!-- 📌 Sidebar -->
        <aside>
          <!-- 📅 ปฏิทิน -->
          <%- include('../partials/calendar') %>

          <!-- 🔐 Login สำหรับสมาชิก -->
          <%- include('../partials/login-box', { loginError: loginError, loginNotice: loginNotice }) %>

          <!-- 👨‍💻 ผู้ดูแลระบบ -->
          <%- include('../partials/admin-box') %>

          <!-- ❓ Q&A -->
          <%- include('../partials/qa-box') %>

          <!-- 📱 โซเชียล -->
          <%- include('../partials/social-box', { qrs: qrs }) %>

          <!-- 📊 สถิติ -->
          <%- include('../partials/stats-box') %>
        </aside>
      </div>
    </section>

    
  </main>

  <!-- ✅ Footer -->
  <%- include('../partials/footer') %>

  <!-- ✅ Script ควบคุม Slider -->
  <script>
    const slider = document.getElementById('slider');
    const prev = document.getElementById('prevBtn');
    const next = document.getElementById('nextBtn');
    let index = 0;
    const slideCount = <%= news.length %>;

    function updateSlider() {
      slider.style.transform = `translateX(-${index * 100}%)`;
    }

    prev.addEventListener('click', () => {
      index = (index - 1 + slideCount) % slideCount;
      updateSlider();
    });
    next.addEventListener('click', () => {
      index = (index + 1) % slideCount;
      updateSlider();
    });
  </script>
</body>
</html>
