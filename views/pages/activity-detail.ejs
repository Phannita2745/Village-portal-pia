<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-blue-50">
  <%- include('../partials/navbar') %>

  <main class="pt-20 max-w-6xl mx-auto px-6 py-8">
    <section class="mt-6 mb-6">
      <!-- แถบหัวข้อ -->
      <div class="bg-green-600 text-white rounded-lg px-6 py-3 mb-6">
        <h1 class="text-2xl font-bold">ภาพกิจกรรม</h1>
      </div>

      <div class="bg-white rounded-lg shadow p-6">
        <!-- ชื่อกิจกรรม -->
        <h2 class="text-xl md:text-2xl font-bold text-center mb-5 text-gray-900">
          <%= item.title %>
        </h2>

        <!-- รูปปก -->
        <div class="flex justify-center mb-6">
          <img
            src="<%= item.image ? ('/uploads/' + item.image) : '/uploads/placeholder.jpg' %>"
            alt="<%= item.title %>"
            class="w-full max-w-sm h-auto object-cover rounded shadow"
          />
        </div>

        <!-- ข้อมูลสรุป (วันเวลา/สถานที่) -->
        <div class="text-center text-sm text-gray-600 mb-6">
          <span>🕒 <%= item.displayDateTime %></span>
          <% if (item.location) { %>
            <span class="px-2">|</span>
            <span>📍 <%= item.location %></span>
          <% } %>
        </div>

        <!-- เนื้อหา: ใช้ details ก่อน ถ้าไม่มีค่อยใช้ description -->
        <% 
          const rawText = item.details || item.description || '';
          const paras = String(rawText).trim() ? String(rawText).split(/\n\s*\n/) : [];
        %>
        <% if (paras.length) { %>
          <div class="space-y-6 text-gray-800 text-base leading-relaxed mb-8">
            <% paras.forEach(p => { %><p><%= p %></p><% }) %>
          </div>
        <% } %>

        <!-- แกลเลอรีรูป -->
        <% if (photos && photos.length) { %>
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-8 mb-10">
            <% photos.forEach(ph => { 
                const src = ph.image_path ? ('/uploads/' + ph.image_path) : '/uploads/placeholder.jpg';
            %>
              <a href="<%= src %>" target="_blank" rel="noopener"
                class="block rounded overflow-hidden shadow hover:shadow-lg transition">
                <img src="<%= src %>" alt="" 
                    class="w-full max-w-xs h-auto object-cover rounded">

              </a>
            <% }) %>
          </div>
        <% } %>

        <!-- แถบข้อมูลล่าง (ถ้ามี views ค่อยแสดง) -->
        <div class="flex justify-end text-sm text-emerald-700">
          <span>🕒 <%= item.displayDateTime %></span>
          <% if (typeof item.viewsPlus !== 'undefined' || typeof item.views !== 'undefined') { %>
            <span class="px-2">|</span>
            <span>👁️ ดู <%= (typeof item.viewsPlus !== 'undefined') ? item.viewsPlus : item.views %> ครั้ง</span>
          <% } %>
        </div>

        <!-- ปุ่มย้อนกลับ -->
        <div class="mt-6">
          <a href="/activities" class="inline-block px-4 py-2 rounded border hover:bg-gray-50">
            ← กลับไปหน้า “ภาพกิจกรรม”
          </a>
        </div>
      </div>
    </section>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
